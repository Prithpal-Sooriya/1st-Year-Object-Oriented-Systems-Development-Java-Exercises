Game: Hangman

write a Hangman game
-that randomly generates a word
-prompts user to guess 1 letter at a time
-each unknown letter --> *, e.g. ****le
-correct guess, display the letter/s (if the is more than 1 character of that letter)
-when they finish the word --> show number of misses
-ask the user if they want to play again
-declare an array to store the words --> String[] words = {............}
- if they repeat a word --> "that letter is already on the board
