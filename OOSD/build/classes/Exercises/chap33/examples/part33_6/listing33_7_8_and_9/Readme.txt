This folder will have 3 listings

listing33_7 = creates a StudentAddress object that is serializable
    --> so we can use the ObjectInputStream and ObjectOutputStream on it

listing33_8 = creates a client class to allow user to fill in the fields for the object
    --> once the field is filled, the object is registered to the server

listing33_9 = creates a server class that reads the object send from client and stores it