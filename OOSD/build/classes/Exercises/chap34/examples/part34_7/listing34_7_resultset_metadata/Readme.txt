TestResultSetMetaData

    --> will create a result set from query:
        --> SELECT * FROM Enrollment;

    --> create a ResultSetMetaData which will return the metadata from Enrollment and query
    
    --> display the contents of the ResultSetMetaData